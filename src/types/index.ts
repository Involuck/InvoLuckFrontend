// This folder is for custom TypeScript types and interfaces.
// For example, the types for Invoice, Client, User, etc.

// Example of an Invoice type:
/*
export interface Invoice {
  id: string;
  invoiceNumber: string;
  clientName: string;
  total: number;
  status: 'draft' | 'sent' | 'paid' | 'overdue';
  createdAt: Date;
  dueDate: Date;
}
*/
